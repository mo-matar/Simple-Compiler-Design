program
  var num: integer;
  var result: boolean;
  function isEven (x : integer) : boolean
    begin
      var remainder : integer;
      remainder := ((x / 2) * 2);
      return((remainder = x));
    end;
  begin
    isEven := 5;
    num(result);
  end;